version: "2"
services:
  srly-ose-viewer:
    image: video-playback
    build:
      context: .
      dockerfile: docker/Dockerfile.vlc
    command: bash
    privileged: true
    restart: always
    stdin_open: true
    tty: true
